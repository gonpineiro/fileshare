import styled from 'styled-components'

export const SvgIcon = styled.svg`
  width: ${({ size }) => size}px;
  margin-left: 7px;
  justify-self: right;
  cursor: pointer;
`

export const SvgTable = styled.svg`
  width: ${({ size }) => size}px;
`